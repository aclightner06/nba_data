from setuptools import find_packages, setup
setup(
    name='nba_data',
    packages=find_packages(),
    version='0.1.0',
    description='Package to lead NBA game and player data.',
    author='a. lightner'
)